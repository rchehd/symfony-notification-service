# This file is a template, and is committed to your repository.
# Create a .env.local file with your actual values for development.
# The .env.local file is ignored by Git.

# --- Symfony Application ---
APP_ENV=dev
# Generate a new secret using 'openssl rand -base64 32' and store it in your .env.local file.
APP_SECRET=

# --- Docker & Development Environment ---
PROJECT_NAME=symfony-notification-service
# The external port for the web server (e.g., access via http://localhost:8080 if set to 8080)
NGINX_PORT=80
# Set XDEBUG_MODE to 'debug' to enable debugging
XDEBUG_MODE='off'
XDEBUG=1
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'

# --- Database & Messenger ---
# Default credentials for the local Docker database.
POSTGRES_USER=app
POSTGRES_PASSWORD=!ChangeMe!
POSTGRES_DB=app

DATABASE_URL="pgsql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?serverVersion=16&charset=utf8"
MESSENGER_TRANSPORT_DSN="doctrine://default?auto_setup=0"

# --- Local Email Testing ---
# Connects to the Mailpit container. View emails at http://localhost:8025
MAILER_DSN=smtp://mailpit:1025

# --- External Service Secrets (add these to your .env.local) ---
# Get your credentials from the Twilio dashboard.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_MESSAGING_SERVICE_SID=
